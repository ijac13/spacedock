---
session-date: 2026-04-17
sequence: 1
first-commit: 1bfadd41
last-commit: 293ad882
duration: ~22h elapsed (overnight break; ~14h active)
---

# Session Debrief — 2026-04-17 #1

A long, hypothesis-correcting session. Started with the lazy-spawn fold-in (#172) and the opus-4-7 cluster (#177/#178), pivoted three times as the diagnosis sharpened: from "ensign hallucination" → "FO-side reply-folding" → "test predicates depended on FO narration verbosity, opus-4-7 narrates more tersely → tests fail." The actual fix landed in #182. No PRs merged this session — three are open and gate-approved awaiting captain.

## Shipped

- **178** `ensign-prompt-tool-call-discipline-boilerplate` — REJECTED + archived. Boilerplate hypothesis falsified by #177's experiments + #182's diagnosis: the opus-4-7 regression was misdiagnosed as ensign tool-call hallucination; the actual issue was test predicates matching FO narration prose. PR #113 closed without merge.
- **180** `model-override-plumbing-fix` — REJECTED-duplicate + archived. Filed as the unblocker for #177's AC-3 BROKEN; implementer audit revealed the bug was already fixed by #179 on main. Closed PASSED-as-duplicate-finding, then archived.

## Filed (backlog)

- **180** `model-override-plumbing-fix` — shipped same session (REJECTED).
- **181** `pin-opus-4-6-ci-default` — pin `--model claude-opus-4-6` in CI workflow defaults until upstream opus-4-7 regression resolves; PR #116 currently open and gate-approved.
- **182** `diagnose-opus-4-7-fo-regression` — local diff against opus-4-6 baseline; PR #117 currently open and gate-approved.
- **183** `ensign-bash-poll-not-sleep` — ensign skill prose to use BashOutput polling instead of blocking sleep for background tasks; observed during #182's iteration cycle.

## Non-PR commits (workflow-only)

State transitions, bounces, ideation cycles, and one design doc that don't belong to a PR:

- `c459cb99` docs: sketch FO-as-API use-cases — brainstorm artifact about code-FO + LLM sidecar architecture (also discussed at length in this session)
- 177 ideation churn: `8010de29` dispatch → `a1e4c16b` ideation → `097ded39` staff-review → `d4342d4b` ideation-revision → `cbc63c82` advance to implementation → `6cf408a7` post-experiment validation → `1b41e6ca` bounce-to-ideation (captain pivot to Layer 2) → `bafd84c8` repurpose-ideation → `3659b07f` repurpose staff-review → `79dab33a` repurpose ideation-revision → `59a0db41` advance to implementation → `5c480923` advance to validation
- 172 churn: `1bfadd41` bounce for fold-in → `a60c3aa0` re-validation
- 181 churn: `b262483e` file → `5eae1ed8` fast-track → `7e1d21d1`/`df4e546b`/`200a8cda` advance/mod-block/PR-open → `6baf6c09` bounce for scope-expansion → `72c20e46` re-validate
- 182 churn: `019ec4ef` file → `dbdf4e22` fast-track → `d8299995`/`a644b632` advance + bounce (captain correction: deliverable is behavioral fix not paper) → `1009014a` advance with behavioral proof → `3fd9c417`/`5af7a822` mod-block + PR-open → `b4de3c22` bounce for FO-impatience extension → `780b320f`/`78cd421a` advance + mod-block (Variant A) → `d48f697b` bounce again for test_feedback_keepalive extension → `1686401e`/`293ad882` advance + mod-block (two-part fix)

## Decisions

- **Reject #178** (boilerplate hypothesis falsified)
- **Close #180** as duplicate of #179 (no code change needed, audit-only closure)
- **Pivot #177** from "test #178 mitigation" → "Layer 2 prompt-shape investigation" → "diagnostic via #182 fast-track" (three pivots)
- **Use #182 for behavioral fix, not diagnosis-only** — captain corrected after FO marked it validation prematurely
- **Bundle find_subagent_jsonl narrowing into #182** instead of separate entity (captain directive)
- **Iterate prose variants up to 3 attempts**; accept ≥4/5 reliability bar
- **Defer then re-include** FO-impatience secondary fix — initial scope-out was the wrong call; expanded back in
- **Expand #182 again** to fix test_feedback_keepalive's opus-4-7 regression after captain noted the same bug class repeated

## Issues — Workflow

- **Test predicates relying on FO narration verbosity** is a recurring class of bug. Found in `test_standing_teammate_spawn.py` (M5 ECHO-ping match) AND `test_feedback_keepalive.py` (expect_exit). `test_gate_guardrail.py` uses `entry_contains_text` similarly — at-risk for the same bug class even though not currently failing.
- **opus-4-7 FO** has at least 3 distinct failure modes: impatience (premature teardown), session re-entry (firing teardown after task_notification), feedback-to teardown (killing implementer during validation). All addressed by Variant A + extension prose; ~50% residual flake remains from a deeper pattern not fully characterized.
- **find_subagent_jsonl** was scanning ~4816 files globally on every `context-budget` call (now narrowed to ~9 via leadSessionId scope in #182).

## Issues — Spacedock

- **pr-merge mod doesn't handle "PR already exists"** — re-push to an open PR works (auto-update), but the merge hook tries `gh pr create` and fails. Worked around manually this session.
- **Ensign skill prose doesn't document BashOutput polling** — leads to `sleep N && tail` anti-pattern, wasting time and making agents uninterruptible. Filed as #183.
- **FO teardown discipline is implicit** in the contract but easily violated by opus-4-7. Now explicit via `## Ensign Completion Signal Discipline` subsection added in #182.
- **Captain-session model stamp `opus[1m]`** doesn't match canonical enum — every reuse check emits a `config_drift_warning`. Cosmetic but noisy.
- **Subagent fo-log artifact** — CI uploads only the parent FO's `fo-log.jsonl`; subagent activity (ensign, standing teammates) lives in their own session logs which aren't preserved. Made the original #177 misdiagnosis possible (only parent log was inspected).

## Observations

- **Diagnosis chain mattered more than mitigation chain.** We spent ~6 hours building #178 boilerplate + 3 hours on #177's first cycle on a wrong diagnosis. A single "actually inspect the ensign sub-agent log" check would have caught it. The #182 specialist diagnostic (with `superpowers:systematic-debugging`) found the real cause in ~1 hour.
- **Prose discipline is fragile against opus-4-7.** Variant 1 was resisted; Variant A worked but required iteration; later test_feedback_keepalive needed yet another two clauses. The architecture brainstorm about code-FO + structured API (vs prose) is increasingly the right answer for this class of regression.
- **Nelson** (`Aspegio/nelson` on GitHub) is a parallel-developed sibling project with named damage-control procedures, risk tiers, and standing orders as separate files. Worth studying — at minimum, named procedures for recurring failure modes (`crew-overrun`, `man-overboard`, etc.) would have shortened multiple cycles this session.
- **comm-officer** was used once successfully (use-cases doc polish); otherwise idle most of the session.
- **The "use #182 to also solve find_subagent_jsonl"** pattern is interesting — bundling narrowly-related fixes into one entity vs. filing separately. Worked here because the surfaces overlapped.

## What's Next

**Captain action required (in priority order):**

1. **Merge PR #116 (#181 — opus-4-6 CI pin).** All 5 jobs green on the PR. Smallest-blast-radius landing; unblocks everything downstream.
2. **Merge PR #117 (#182 — opus-4-7 fix).** Once #181 lands, re-trigger CI on #117 (or just merge as-is — its PR-trigger CI ran; opus-4-6 verification PASSED via workflow_dispatch).
3. **Rebase #172 onto post-#181-merge main, push to update PR #107, captain merges.** Lazy-spawn finally lands.
4. **Local-merge #177 entity body + archive.** No PR needed (entity-body audit only).
5. **Decide #058's disposition** — terminology experiment, sitting at validation since prior session.
6. **Decide #088's disposition** — restore initialPrompt PR #36 still open from before this session.
7. **Decide #153's disposition** — codex completion notifications, in-flight implementation.

**Backlog needing ideation/dispatch:**

- High-value: #119 FO dispatch Phase 1 band-aids, #152 pr-merge CI awareness, #154 refresh live-test assertions, #147 FO issue-filing guardrail enforcement
- Cluster cleanup: #163 pr-merge regate-on-PR-failure, #164 claude-team build path-doubling, #165 entity-state sidecar
- Long-term: #168 session self-diagnosis, #169 experiment workflow direction, #170 first-boot measurement harness, #167 relocate status/claude-team scripts

**Deferred this session, worth re-surfacing:**

- #182 has a documented ~50% residual opus-4-7 flake on standing-teammate test (separate FO behavior pattern not addressed by Variant A's prose). Could become a follow-up entity for code-mechanism fix.
- The "narration-vs-data-flow test predicate" bug class — `test_gate_guardrail.py` is at-risk; should be audited.
- Nelson-inspired adoption: file an entity for "study Nelson's damage-control library + adopt named procedures."
- Ideation-quality README updates discussed in brainstorm — not yet filed.

---

## Handoff Note

**State to load on boot:**

- Three PRs open and gate-approved awaiting merge: #116 (#181), #117 (#182), #107 (#172). Merge order: **#181 → #182 → rebase #172 → #172**.
- Workflow has 7 in-flight entities total; #172/#177/#181/#182 are this session's; #088/#058/#153 carried over from prior.
- comm-officer alive in team; if next session reuses the team, lazy-spawn semantics from #172 (once landed) skip eager re-spawn.

**Cluster context that won't be obvious from frontmatter:**

- The opus-4-7 regression is real but workaroundable: (a) #181 pins CI to opus-4-6, (b) #182's prose tightens FO discipline, (c) ~50% residual flake remains for which the right fix is mechanism-level (the brainstorm direction — code-FO with API surface).
- Test predicate audit needed: at minimum `test_gate_guardrail.py` uses `entry_contains_text` similar to the bugs we fixed. Likely a broader pattern.
- The session burned significant time chasing the wrong diagnosis (parent-fo-log misread). Future opus-4-7-class diagnostics should preserve subagent fo-logs and inspect them BEFORE forming hypotheses.

**Honest note about FO behavior this session:**

- This FO (also opus-4-7) made several scope-tracing slips that captain caught and corrected: over-attribution to opus-4-7 in mid-session brainstorm response (captain pushed back; legit overclaim), narrow #181 scope on first pass (missed make-target path), premature jump to validation on #182, marked one validator dispatch before bouncing back. Pattern: had to be re-scoped multiple times. Worth the next session keeping watch for similar drift.

**Most important single thing for next session:**

The architecture brainstorm doc at `docs/superpowers/specs/2026-04-17-spacedock-fo-as-api-use-cases.md` (committed `c459cb99`) is the design direction emerging from this session's pattern of opus-4-7 prose-discipline fragility. The 5 use cases there ("why is this stuck?", "what's on my desk?", crash-safe continuation, hallucination-resistant state mutations, headless/cron) all point at the same architectural lever: code-FO + narrow API surface. Nelson is a sibling reference implementation. This is the next major design decision for spacedock as a project, not just a workflow improvement.

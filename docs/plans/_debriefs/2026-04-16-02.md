---
session-date: 2026-04-16
sequence: 2
first-commit: 0e3d2a88
last-commit: 6df33776
duration: ~9h active (spanning merges from prior day)
---

# Session Debrief — 2026-04-16 #2

Marathon afternoon/evening session. 7 entities shipped through PRs (including one external contribution on the marketplace schema), 15+ new tasks filed, deep dive into the opus-4-7 regression. Headlined by bisecting Claude Code 2.1.110→2.1.111's default-alias flip (`--model opus` now resolves to `claude-opus-4-7`), catching the model fabricating tool-call outcomes in stage reports, shipping the streaming FO watcher (#173) that catches such regressions in ~75s instead of 600s, then layering mitigations: CI bisection inputs (#174), dated-model pin (#176/#179), and prose discipline boilerplate (#178, PR still open).

## Shipped

- **162** `standing-teammate-mod-hook` — [#102](https://github.com/clkao/spacedock/pull/102). Ship the "standing teammate" pattern so Spacedock workflows can declare long-lived specialist agents via mod `standing: true`, spawned once per captain session.
- **166** `standing-teammate-enumeration-usage-payload` — [#103](https://github.com/clkao/spacedock/pull/103). Inline per-teammate `## Routing Usage` spec into dispatch prompts plus `claude-team list-standing` helper-side enumeration so ensigns discover trigger phrases without opening the mod file.
- **174** `ci-workflow-dispatch-bisection-inputs` — [#108](https://github.com/clkao/spacedock/pull/108). Three optional workflow_dispatch inputs (`claude_version`, `test_selector`, `effort_override`) to keep bisection in CI instead of requiring commits.
- **173** `streaming-fo-watcher-per-step-timeouts` — [#109](https://github.com/clkao/spacedock/pull/109). FOStreamWatcher tails fo-log.jsonl during execution, asserts milestones with per-step timeouts, surfaces red-CI failures in ~75s instead of ~600s. Cycle 1 validation rejected (live smoke showed fail-fast didn't actually fail fast); cycle 2 added exception-exit fast-terminate.
- **176** `ci-workflow-model-override-input` — [#110](https://github.com/clkao/spacedock/pull/110). Fourth bisection input: explicit `--model` override for claude jobs, bypassing default-alias resolution.
- **175** `migrate-live-tests-to-streaming-watcher` — [#111](https://github.com/clkao/spacedock/pull/111). Migrate 6 live tests (gate_guardrail, merge_hook_guardrail, feedback_keepalive, team_dispatch_sequencing, dispatch_names, rebase_branch_before_push) to the streaming watcher pattern from #173.
- **179** `fix-extra-args-model-plumbing-in-migrated-tests` — [#112](https://github.com/clkao/spacedock/pull/112). Plumb pytest's `model` fixture into `extra_args` for the 2 #173 pilot tests (they hardcoded `--model opus`, silently breaking #176's `model_override`).
- **[external]** `marketplace.json schema fix` — [#115](https://github.com/clkao/spacedock/pull/115). Karen Hsieh (@ijac13) diagnosed and fixed schema validation error blocking `claude plugin marketplace add clkao/spacedock`. Continued their branch with authoritative-file + test updates to close #114.

## Filed (backlog)

- **163** `pr-merge-mod-regate-on-pr-path-failure` — PR-path failure in the pr-merge mod should re-gate instead of auto-falling back to local merge.
- **164** `claude-team-build-path-doubling` — `claude-team build` doubles `.worktrees/` prefix when `entity_path` is passed as a worktree-absolute path.
- **165** `entity-state-sidecar-file` — move entity current-state out of frontmatter into a sidecar file to reduce main-branch state-churn commits.
- **167** `relocate-status-and-claude-team-to-first-officer-skill` — both scripts are FO runtime tooling, not commission scaffolding (includes fold-in of #164 and the `claude-team build` `It contains:\n` dangler).
- **168** `session-self-diagnosis-skill` — tooling to avoid rederiving jq pipelines every diagnostic.
- **169** `experiment-workflow-direction` — autoresearch optimization loop placement (shared repo / external / hybrid).
- **170** `first-boot-measurement-harness` — wallclock/turns/token cost across project archetypes.
- **171** `agent-model-override-teams-mode` — `Agent(model=X)` teams-mode propagation bug (pre-existing, amplified by opus-4-6 → opus-4-7 cutover).
- **172** `lazy-standing-teammate-spawn` — defer standing-teammate spawn to first dispatch, not boot. Shipped same session (PR #107).
- **173** `streaming-fo-watcher-per-step-timeouts` — shipped same session.
- **174** `ci-workflow-dispatch-bisection-inputs` — shipped same session.
- **175** `migrate-live-tests-to-streaming-watcher` — shipped same session.
- **176** `ci-workflow-model-override-input` — shipped same session.
- **177** `opus-4-7-ensign-hallucination-scope` — opus-4-7 fabricates tool-call outcomes in stage reports at low/medium effort; scope affects every ensign dispatched, not just standing teammates.
- **178** `ensign-prompt-tool-call-discipline-boilerplate` — cheapest prose-level mitigation; add always-on discipline section to every `claude-team build` output. PR #113 open, smoke positive.
- **179** `fix-extra-args-model-plumbing-in-migrated-tests` — shipped same session.

## Non-PR commits (workflow-only)

State transitions and scaffolding that did not flow through a PR:

- `78c39d6a` xfail `test_per_stage_model_haiku_propagates` citing #171 (platform bug tracked but CI unblocked).
- `9d543eba` **[reverted]** marketplace.json schema fix. Pushed directly to main before spotting external contributor's PR #114 already open.
- `e76f2268` Revert "fix: marketplace.json schema" — reverted the direct push so Karen's PR #114 commit could be preserved as primary author via continued-branch in #115.
- `1e019cf1` update: #167 fold `claude-team It contains:\n` dangler into scope (captain-guided mid-session scope expansion).
- `3dbb3787` update: #177 add high/xhigh evidence after CI bisection showed effort bump exposes a second failure mode.
- `08e8a755` update: #179 correct scope from 8 files to 2 (initial draft overstated the surface).
- `11aea424` update: #171 correlate with 2.1.110→2.1.111 default-alias flip + distinguish from standing-teammate regression.

All other session commits are rolled up in the shipped PRs above.

## Decisions

1. **Fast-track past ideation** became a recurring pattern — used for #174, #176, #178, #179 where the design was either trivial or spelled out fully in the entity body. Saves ~10-20 min per task but requires a tight spec and captain willingness to own the gate.
2. **Skip separate validation** for CI workflow changes (#174, #176) — CI-on-CI is chicken-and-egg. Offline YAML lint + post-merge spot-check is sufficient.
3. **Captain override on validation gates** — #173 cycle 1 validation reported ACCEPTED but live smoke showed the fail-fast promise was broken; override → reject → cycle 2 fix. Lesson: for behavioral-mitigation tasks, live smoke matters more than offline unit tests. Re-applied in #178 where live smoke ran before merge decision.
4. **External contributor attribution** — when I pushed the marketplace fix directly 6 minutes after PR #114 opened, we reverted the direct push and continued Karen's branch as PR #115 to preserve her authorship. Worth the churn for the respect.
5. **Dated-model pin as primary opus-4-7 mitigation** — effort bump to high/xhigh trades one failure mode for another; the dated-model pin (`--model claude-opus-4-6`) is the only tested clean fix. Medium effort FAILED in CI despite local run passing at the wire.

## Issues — Workflow

None identified. The workflow ran well — 7 ships, clean feedback cycles, gate discipline held. comm-officer remained useful throughout for polish and routing.

## Issues — Spacedock

1. **opus-4-7 ensign hallucination at low/medium effort** — captured in #177. Affects every ensign dispatched via `claude-team build` when the captain session runs on `opus-4-7` (default on Claude Code 2.1.111+). Mitigations in flight: #178 boilerplate (PR open), #176 model-override (merged, awaiting CI proof), #171 (platform-issue tracker).
2. **FO-level hallucination at opus-4-7 low effort** — new finding during #178 smoke: the FO itself shuts down teammates before their round-trips resolve. Needs an FO-side discipline layer analogous to #178 but for the FO's own prompt (likely in shared-core or runtime adapter). Should be folded into #177's ideation.
3. **`Agent(model=X)` teams-mode propagation** — #171. Distinct from #177 but related to same opus-4-7 cutover.
4. **Milestone 5 test-assertion shape bug** — `test_standing_teammate_spawn`'s `ECHO: ping` assertion checks the parent fo-log, which relied on opus-4-6's loquacity; opus-4-7 is terse and does not relay teammate messages in its own text. Should convert to post-exit entity-file-contents check. Not filed yet.
5. **marketplace.json schema** — fixed via PR #115 (external contributor). Our `test_codex_marketplace_matches_approved_contract` was pinning the broken schema as "approved contract" — should validate against Claude's actual schema, not just format drift. Partially addressed in #115 but full-schema validation remains open.
6. **`claude-team build` path-doubling** — #164.

No spacedock issues filed upstream this session (all tracked in entity files).

## Observations

1. **The streaming watcher paid for itself in the same session.** PR #107 opus-low failure took 24m38s pre-migration; the same failure on 2.1.111 opus/medium took 5m09s with the migrated test. The 24m-to-75s promise verified in production CI, not just in offline unit tests.
2. **First external contributor.** Plugin marketplace install path had been broken since the codex plugin scaffold (10f67572) shipped. Karen's PR #114 arrived ~6 minutes before my direct fix. Handling the conflict as a revert-and-continue-their-branch (PR #115) preserved her attribution. Worth documenting as a pattern.
3. **Opus-4-7 migration-guide predictions held for ensigns but broke for the FO.** The guide's "at low and medium, the model scopes its work to what was asked" was directly observed in #177. But the guide's "raise effort to high or xhigh rather than prompting around it" was partially contradicted by #178 smoke: prose discipline at low effort DID change ensign behavior (ensign emitted SendMessage correctly). The FO on opus-4-7 at low effort still hallucinated completion even with the new boilerplate in scope (because the boilerplate only rides in dispatched ensign prompts, not in the FO's own skill).
4. **Session cost pattern:** 7 PRs shipped, ~15 tasks filed, 91 scoped commits. Fast-tracking past ideation accelerated 4 of them.
5. **Mitigation matrix for opus-4-7 is complete but asymmetric.** Three axes tried: dated-model pin (works cleanly, CI proof pending), effort bump (trades failure modes), prose discipline (works at ensign level, needs FO-level equivalent). Dated-model pin + prose discipline together is likely the right v1 default.
6. **Debrief format is slightly stale.** The approved-contract test drift (issue 5 above) is an example of "tests that look green but pin broken content." Worth noting as a category — our offline tests can hide schema validation failures if the tests pin the broken shape rather than validating against the spec.

## What's Next

### For #177 (opus-4-7 ensign hallucination scope)

- **Backlog, score 0.85.** Not dispatched yet.
- **Ideation should resolve:** which mitigations ship as spacedock defaults vs user-opt-in. The options are now well-mapped:
  - Prose discipline (#178, open as PR #113, live smoke positive at opus/low)
  - Dated-model pin (#176 enables; #112 landed the plumbing; in-CI proof still pending)
  - Effort bump to high/xhigh (partial — trades failure mode rather than fixing; likely not recommended)
  - FO-side post-completion verification (cross-check stage report's DONE markers against stream evidence)
- **New evidence to fold in:** the opus/low smoke with #178 showed the FO ALSO hallucinates completion (shuts down teammates prematurely), not just ensigns. #177's scope may need to expand to include an FO-level discipline layer.
- **Staff review warranted** (score ≥ 0.8 + touches ensign prompt template + affects production usage).
- **Post-merge CI smoke pending:** the in-CI mitigation proof (`model_override=claude-opus-4-6` on 2.1.111) — run 24541789653 — should return before ideation is dispatched. If it passes in ~2-3 min, the dated-model pin option moves from "likely works" to "proven."

### For #178 (ensign-prompt tool-call-discipline boilerplate)

- **PR #113 open, awaiting merge decision.**
- **Live smoke at opus/low done — positive:** ensign emits `SendMessage` correctly and reports failure honestly instead of fabricating success. Milestone 5 still fails but that's the test-assertion shape bug (issue 4), not #178's fault.
- **Live smoke at opus/high done — neutral:** milestone 4 passes (same as no-boilerplate at high), milestone 5 still fails (same test-shape issue). Boilerplate does not regress high-effort behavior.
- **Scope-adjacent touch flagged:** implementation loosened a `"SendMessage" not in prompt` bare-word check in `test_claude_team.py::TestBuildBareMode` because the new always-on boilerplate legitimately names SendMessage as an example tool. Defensible, documented in commit.
- **Recommendation on merge:** merge directly. Positive smoke signal is sufficient. Cost of being wrong is one-line revert; cost of holding is continued ensign fabrication on every opus-4-7 captain session.

### Other ready for dispatch

- **#170** `first-boot-measurement-harness` — well-scoped, ready for ideation.
- **#168** `session-self-diagnosis-skill` — well-scoped, ready for ideation.
- **#167** `relocate-status-and-claude-team-to-first-officer-skill` — well-scoped, touches scaffolding so would benefit from ideation.

### Deferred / lower priority

- **#169** `experiment-workflow-direction` — placement decision; CL may prefer to prototype before filing a direction.
- **#164**, **#165** — smaller bugs/observations, pick up when relevant.
- **#171** — platform issue, still xfailed in tests, awaiting upstream fix or workaround from #177 ideation.

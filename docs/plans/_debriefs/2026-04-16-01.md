---
session-date: 2026-04-16
sequence: 1
first-commit: ac015d3b
last-commit: 0f43b855
duration: ~44h
---

# Session Debrief — 2026-04-16 #1

Long session spanning ~44 wallclock hours (most async). Six entities shipped (five via PR, one via direct merge from a concurrent session); eight new entities filed (one shipped same session, one in-flight on PR, one sequestered to a branch, five in backlog). Thematic work: dispatch-layer mechanism hardening — Agent `model=` parameter honored from workflow declarations (#157), FO staleness-echo prose discipline shipped (#159), standing-teammate mod pattern with build-time auto-enumeration designed and landed (#162, PR open). In-session pilot of `comm-officer` ran through four mod revisions and validated a new polish-and-write mode shape; several follow-ups filed for discoverability and architectural concerns that surfaced.

## Shipped

- **144** `workflow-readme-schema-and-fields-deduplication` — [#91](https://github.com/clkao/spacedock/pull/91). Drop the `## Schema` YAML block from commissioned READMEs; `### Field Reference` table alone carries field metadata.
- **148** `live-e2e-pytest-harness` — [#94](https://github.com/clkao/spacedock/pull/94). Migrate live E2E tests to pytest with `live_claude` / `live_codex` markers and serial/parallel tiers.
- **149** `fo-team-infrastructure-fail-early` — [#97](https://github.com/clkao/spacedock/pull/97). FO no longer pre-dispatch-probes `test -f config.json`; never retries TeamCreate to the same name; first-class bare-mode fallback with fresh-suffixed team names.
- **157** `claude-team-respect-stage-model` — [#100](https://github.com/clkao/spacedock/pull/100). `claude-team build` emits effective model from `stages.defaults.model` / per-state override; FO forwards to `Agent(model=...)` (closes upstream clkao/spacedock#95).
- **159** `status-set-staleness-echo-mitigation` — [#101](https://github.com/clkao/spacedock/pull/101). Shared-core adds "prefer Grep over Read for entity inspection"; `status --set` stdout emits `field: old -> new` per change (closes upstream clkao/spacedock#96).
- **142** `codex-plugin-scaffold-for-spacedock-install` — direct-merge on main (concurrent session, no PR). Ships codex-plugin packaging target + tests.

## Filed (backlog)

- **158** `pr-merge-hook-haiku-skips-rebase` — haiku-driven FO silently drops `git rebase main` before branch push, observed in PR #94 CI.
- **159** `status-set-staleness-echo-mitigation` — shipped same session (see above).
- **160** `haiku-fo-multi-dispatch-compression` — haiku FO collapses "work + review" dispatch pair into one Agent() call, observed in PR #97 CI.
- **161** `codex-reused-wait-text-drift` — codex reused-worker wait-text assertion drifts on routed-feedback-fix, observed in PR #97 CI.
- **162** `standing-teammate-mod-hook` — mod-declared long-lived teammates auto-enumerated into every dispatch prompt. **PR #102 open**, cycle 2 approved at validation gate.
- **164** `claude-team-build-path-doubling` — `claude-team build` path-doubles when `entity_path` is passed as a worktree-absolute path; deterministic bug observed during #162 dispatch.
- **165** `entity-state-sidecar-file` — architectural exploration: move entity current-state out of frontmatter into a sidecar to reduce main-branch state-churn commits. Problem + four options, no solution pre-committed.
- **166** `standing-teammate-enumeration-usage-payload` — follow-up on #162 AC-13: the auto-enumeration section's per-teammate wording ("SendMessage with the relevant input shape") is too thin for dispatched workers to actually invoke non-trivial modes.

## In flight

- **162** `standing-teammate-mod-hook` — validation cycle 2 PASSED; PR #102 OPEN with `mod-block: merge:pr-merge`. Will advance + archive on next session when merged upstream.

## Sequestered

- **163** `kilocode-support` — filed, ideated, implemented in a concurrent session running directly on main. Extracted onto branch `kilo/163-kilocode-support` via interactive rebase + force-push on main (10 pushed + 5 local #163 commits stripped). Branch captures the full #163 history plus the `.kilo/` runtime scaffolding as uncommitted-then-committed files. `backup/main-before-163-cleanup` kept local as recovery point.

## Non-PR commits (workflow-only)

State transitions and scaffolding that don't belong to any PR:

- `ce60505c` release: bump to spacedock@0.9.6 + `e0d800ee` refit: upgrade workflow scaffolding to spacedock@0.9.6. Version bump propagation.
- `9b0c9fe1` refactor: inline AC-4 Codex probe evidence into #159 entity body (removed separate `.probe-evidence/` file per captain direction; one-file audit).
- **#163 extraction work.** Two-commit sequence: first force-rewrite on main stripping 10 already-pushed #163 commits; then `kilo/163-kilocode-support` branch creation with full history + `.kilo/` capture commit. Backup branch `backup/main-before-163-cleanup` preserved locally.
- **#153 `codex-completion-notifications-must-preempt-side-discussion` escalated to cycle 3.** `64c71d84` feedback cycle + `15c5fcc4` upstream codex issue note. Task remains stuck at implementation; needs captain triage before further work.
- **comm-officer mod pilot.** Mod filed alongside #162 (`c6a91639`), iterated four times in-session: `b81464f6` Finding D (disambiguating-context preservation), cycle-2 respawn baking Findings A/B/C, `57fb712f` added polish-and-write + polish-and-edit modes mirroring Write/Edit tool shapes. Polish-and-write smoke-tested green end-to-end.

Feedback-cycle commits for shipped entities (#157 two rejections + probe pivot; #162 one rejection triggering Fix 2) are rolled up in the Shipped section's PR links; not enumerated individually. All other session commits are routine state churn (`dispatch:`, `advance:`, `mod-block:`, `task:`) captured by the Shipped / Filed sections.

## Decisions

- **Rejected #157 ideation twice** citing unverified runtime assumptions (cycle 1: `Agent(model=...)` presumed-absent; cycle 2: `{workflow_dir}/.claude/agents/` presumed-discovered). Directed empirical probes before design commitments. Probe-discipline lesson ("read schema before greping callers") subsequently shipped as a shared-core rule in #157's own implementation.
- **Approved #157 ideation** after cycle 4 refinement incorporated probe-verified evidence (`Agent(model="haiku")` stamps correctly in team config).
- **Rejected #162 validation cycle 1** citing Finding E: AC-10 shipped FO-side polish routing but left ensign-side routing as discipline burden. Directed mechanism-over-discipline Fix 2 instead of a prose patch.
- **Approved #162 validation cycle 2** after auto-enumeration mechanism landed + dogfood (live comm-officer + scratch team) + anti-gaming spot-check all passed.
- **Directed: pilot comm-officer as a local mod first** before committing to the standing-teammate abstraction. Four pilot rounds validated the mod shape and surfaced Findings A-D now baked into the mod prompt.
- **Directed: polish scope** = drafts + entity-body contents, NOT live captain chat. Baked into comm-officer's routing guidance.
- **Directed: sequester #163 kilocode-support to a branch.** Main was rewritten via interactive rebase and force-push to strip all #163 commits (pushed + local).
- **Directed: #165 state-sidecar task should frame problem + options, not pre-commit to a solution.** Recommended v1 direction removed; backlog-filed tasks stay problem-focused, ideation commits to an option.
- **Directed: #166 discoverability fix as follow-up on #162, not fold into #162.** #162 is gate-approved; amendments post-approval feel wrong.

## Issues — Workflow

- **Haiku FO compresses multi-step flows.** Observed twice this session: pr-merge hook skips `git rebase main` (→ #158), multi-stage dispatch collapses "work + review" into one Agent() call (→ #160). Pattern: haiku under dense FO prose drops intermediate steps. Likely structural fix rather than prose tightening.
- **Codex live-harness flakiness.** 600s wall + echo-agent early-teardown observed across #148 and #162 validator runs. Pre-existing behavior; deserves its own follow-up task if it persists.
- **Stale `test -f config.json` probe in FO session.** Captain corrected me mid-session for running the Rule-1-retired pre-dispatch probe after #149 merged mid-session. Root cause is below (Spacedock issue).

## Issues — Spacedock

- **Mid-session merges of shared-core / runtime-adapter changes don't propagate to the running FO's prose.** The FO's instruction set is loaded once at session boot. When #149's Rule 1 landed mid-session, my own behavior continued to follow pre-#149 prose for the rest of the session. Ensigns re-read files per dispatch (via `claude-team build`); the FO doesn't. Two candidate mitigations: (a) session-restart hook after self-merges, (b) self-diff check at key decision points comparing loaded-prose to current-on-disk. Worth filing — this will keep biting.
- **`claude-team build` path-doubles when `entity_path` is worktree-absolute.** Filed as #164. Small helper input-handling bug, deterministic reproduction.
- **FO discipline gaps around surfacing standing teammates to dispatched workers.** #162 Finding E was the motivating failure ("FO forgets to add polish-routing opt-in to dispatch briefs"). Mechanism-over-discipline pattern surfaced as a general review heuristic: if the FO has to remember to do something consistently, a mechanism beats a prose patch. Related follow-up filed as #166 (discoverability payload).
- **Probe methodology rule** — "usage presence is not existence evidence" shipped via #157 as a shared-core bullet. Generally applicable; not an open issue.

## Observations

- **4 ideation cycles + 3 staff reviews on #157** was the cost of not running empirical probes up front. Cycles 1-3 invented increasingly elaborate workarounds for a problem that didn't exist (the Agent `model=` parameter was documented in the tool schema all along). Cycle 4 refinement was additive and cheap. Next time: probe before designing around an absence.
- **comm-officer pilot findings (baked into mod prompt):**
  - Finding A: skill-availability warning on spawn + in every reply's `Guide applied` field — no silent degradation.
  - Finding B: reply-format hard rules — body IS the deliverable, no summary-only messages.
  - Finding C: each SendMessage is discrete — no "inline above" or "attached" language.
  - Finding D: preserve disambiguating attributions and parenthetical modifiers; don't change semantic qualifiers silently.
- **Mechanism-over-discipline is the session's architectural theme.** #149 Rule 1 (no pre-dispatch probe), #157 (probe schema before grep), #159 (Grep over Read + status set stdout diff), #162 Fix 2 (auto-enumerate teammates). All pattern: if discipline is brittle, ship a mechanism.
- **Mid-session FO prose staleness asymmetry.** Ensigns re-read scaffolding files per dispatch via `claude-team build`; FO's loaded-at-boot context is sticky. Asymmetric cost for the same files. Worth filing if it bites again.
- **State-commit noise on main during worktree-backed stages** surfaced as an architectural concern when #163's concurrent session forced a force-push extraction. #165 captures the four options; ideation will pick.
- **comm-officer interaction modes evolved this session**: text passthrough → file-in-place (pilot shape) → polish-and-write (smoke-tested green today) → polish-and-edit (defined but not yet smoke-tested). Discoverability of the new modes is the next gap, filed as #166.
- **Concurrent FO sessions are a real operational mode.** The #142 and #163 work ran in parallel with this session's #157/#159/#162 work. Main-branch commits interleaved; no data loss but history became noisy. Spacedock's workflow-scoped-team assumption doesn't gracefully compose across captain sessions.

## What's Next

**Ready to advance when PR #102 merges upstream:**
- **#162** — PR open, mod-block set. Startup hook archives on merge.

**Stuck or needs captain decision:**
- **#153** `codex-completion-notifications-must-preempt-side-discussion` — status implementation, escalated to cycle 3 feedback this session. Needs captain triage before further work.
- **#088** `restore-initial-prompt` — validation, PR #36 OPEN upstream, worktree gone. Long-stalled since 2026-04-06.
- **#058** `terminology-experiment` — validation, worktree stale since 2026-03-27. Candidate for decision: clear worktree field, reject, or revive.
- **#143** `claude-team-health-subcommand` — likely superseded by #149; candidate for archive-as-superseded.
- **#163** `kilocode-support` — sequestered on branch `kilo/163-kilocode-support`, not on main. Future session decides whether to pick up or drop.

**Top-ranked ideation backlog:**
- **#119** `fo-dispatch-phase-1-band-aids` (0.75)
- **#152** `pr-merge-mod-ci-awareness` (0.75)
- **#154** `refresh-live-test-assertions-after-skill-preload` (0.70)
- **#165** `entity-state-sidecar-file` (0.70) — filed this session
- **#142** `codex-plugin-scaffold-for-spacedock-install` — already shipped via direct merge

**Filed but un-triaged backlog** (from this session):
- #158, #160, #161 (CI-surfaced bugs, lower-priority than the above)
- #164 (helper input-handling bug, small)
- #166 (follow-up on #162 discoverability)

**Unfiled observations worth tasks if they recur:**
- Mid-session FO prose staleness asymmetry (Spacedock issue).
- Live-harness 600s wall + echo-agent teardown flake class.

**Standing teammate left alive:** `comm-officer` in team `spacedock-plans-2`, sonnet, elements-of-style skill armed, four-mode prompt (v3). Available for polish routing in the next session if the team persists.

---
session-date: 2026-04-18
sequence: 1
first-commit: 99fb7e01
last-commit: 4dd5b448
duration: ~15h active (spanned overnight into morning)
---

# Session Debrief — 2026-04-18 #1

Session opened with 3 PRs gate-approved awaiting merge (#181/#182/#172) and landed 7 shipped entities + 11 new filings, including a clean carve-out of #182's mergeable portions into #184/#185 and a major fork-PR-testing overhaul (#189 `pull_request_target` + env-gate). Two significant framing upgrades surfaced mid-session — checklists as per-dispatch linchpins (#192), and ACs as entity-level properties (#193 ideation) — both of which retroactively explained several earlier scope-drift incidents.

## Shipped

- **181** `pin-opus-4-6-ci-default` — [#116](https://github.com/clkao/spacedock/pull/116). Pin the CI `claude-opus` live-test default from `opus` (now resolving to opus-4-7) to `claude-opus-4-6` to sidestep the opus-4-7 ensign regression while it's investigated.
- **172** `lazy-standing-teammate-spawn` — [#107](https://github.com/clkao/spacedock/pull/107). Defer standing-teammate spawn from FO boot to first routing, eliminating cold-start cost for sessions that never dispatch team work.
- **184** `cherry-pick-claude-team-narrowing` — [#121](https://github.com/clkao/spacedock/pull/121). Narrow `claude-team`'s subagent JSONL scan from a global `~/.claude/projects` glob to the team's own session directory — 4863→9-16 files per lookup on a populated machine.
- **182** `diagnose-opus-4-7-fo-regression` — [#117 closed-rejected](https://github.com/clkao/spacedock/pull/117). Closed REJECTED for scope drift; the diagnostic content was preserved and mergeable portions carved to #184/#185/#186.
- **183** `ensign-bash-poll-not-sleep` — [#122](https://github.com/clkao/spacedock/pull/122). Add a `## Background Bash Discipline` subsection to the ensign shared-core so agents use `BashOutput` polling on background commands instead of blocking `sleep`.
- **185** `cherry-pick-test-predicates-and-audit` — [#123](https://github.com/clkao/spacedock/pull/123). Replace brittle `entry_contains_text` narration predicates with data-flow assertions and `pytest.xfail` the bare-mode haiku combination whose shortcut paths violate the test's keepalive assumptions.
- **189** `fork-pr-live-e2e-pull-request-target` — [#124](https://github.com/clkao/spacedock/pull/124). Switch `runtime-live-e2e.yml` from the `workflow_dispatch + pr_number` cargo-cult pattern (which always tested `main`) to `pull_request_target + env-gate` so live-e2e actually runs against PR-head code, for both same-repo and fork PRs.
- **192** `limit-fo-checklist-to-three-items` — [#126](https://github.com/clkao/spacedock/pull/126). Cap FO-built dispatch checklists at ≤ 3 linchpin items; prose-only rule in first-officer shared-core, guarded by a static grep assertion.

## Filed (backlog)

- **184** `cherry-pick-claude-team-narrowing` — shipped same session.
- **185** `cherry-pick-test-predicates-and-audit` — shipped same session.
- **186** `green-opus-4-7-full-suite` — enumerate and fix opus-4-7-specific failures so the pin can eventually be lifted; implementation in flight (cycle 5 bounced; cycle 6 queued).
- **187** `refine-entity-as-folder-pr118` — track Karen Hsieh's external PR #118 (entity-as-folder support) through our review standards while preserving contributor attribution; ideation at gate.
- **188** `streaming-watcher-over-filesystem-polling` — shipped same session (PR #127 in flight at debrief time).
- **189** `fork-pr-live-e2e-pull-request-target` — shipped same session.
- **190** `test-feedback-keepalive-two-path-assertion` — rewrite `test_feedback_keepalive`'s core assertion to handle both dispatch and inline-process FO paths meaningfully (follow-up to #185 cycle 3 xfail).
- **191** `revisit-commission-skill-behavior` — revisit commission skill for first-time vs experienced user personas.
- **192** `limit-fo-checklist-to-three-items` — shipped same session.
- **193** `separate-ac-from-checklist-entity-vs-stage` — distinguish entity-level acceptance criteria from stage-level checklists; ideation complete, staff review pending.
- **194** `opus-4-7-standing-teammate-roundtrip-flakiness` — investigate opus-4-7 FO flakiness on `test_standing_teammate_spawn` ECHO roundtrip (carved from #188 AC-5 evidence).

## Non-PR commits (workflow-only)

State transitions, scaffolding, and direct-to-main CI work that don't belong to a PR:

- `a7b528e5` **merge: #183 + #172 done PASSED** — direct-merged both via GitHub UI (captain bypassed pr-merge mod after CI green); terminal reconcile committed here.
- `40e72602` **merge: #182 done REJECTED** — closed PR #117 + archived + worktree cleanup; scope-drift rejection.
- `fa7abb60` **merge: #184 done PASSED** — late idle-hook-catch reconcile; PR #121 had merged at 01:06Z but entity remained at validation until ~03:51Z.
- `8ba924b6` **ci: gate live-e2e jobs on static-offline via `needs:`** — direct-on-main workflow edit; prevents wasted env-approvals on runs that would fail static.
- `6b8456b1` **ci: remove ci-static.yml** — runtime-live-e2e's internal static-offline job is now the single source; contract tests retired.
- `e8fd865e` **condense: #186 Feedback Cycles** — FO-owned entity body housekeeping (292→246 lines).
- `d967ee43` **note: #188 context-exhausted subagent resisted shutdown** — captured in `_debriefs/_pending-notes.md` for future investigation.
- Feedback-cycle bounces on #185: `6ef667aa`, `5a26470e`.
- Feedback-cycle bounces on #186: `593e6e3f`, `949791ee`, `f64f85cb` (ideation cycle 1 after staff review; cycle 3 AC-7 unpin-scope expansion; cycle 4 "1× full-suite" captain reframe).
- Ideation stage-report commits on #185/#186/#188/#192/#193: `2e20aee4`, `ae0c0677`, `bdc79706`, `d9e5c347`, `751f9d03`, `8417f50b`, `38e7dd76`, `1f99bb27`, `6f50061a`, `dc785f89`.

All other session commits are rolled up in the shipped PRs above.

## Decisions

_(none recorded)_

## Issues — Workflow

- **AC vs checklist conflation surfaced.** #186 cycle 3 "deferred" AC-5 (`3× test_standing_teammate_spawn`) as "a 10-minute validation-stage follow-up" — treating an entity-level acceptance criterion as a stage-level to-do. CI immediately hit exactly what AC-5 would have caught. This prompted #193's framing distinction and the discipline statement: *"AC = entity-level done-when. SKIPPED requires captain-approved rationale before the skip."*
- **#185 cycle-3 xfail scope was too narrow.** The `pytest.xfail` only covered `--team-mode=bare AND --model=claude-haiku-4-5`. Later CI on #118 showed the same third-watcher failure on non-bare claude-live opus-4-6. Remediation route: #190 (rewrite assertion to cover both dispatch and inline-process paths).
- **`test_gate_guardrail` silently ran on sonnet, not opus-4-7.** The test signature doesn't take a `model` fixture; `tests/test_gate_guardrail.py:48-52` passes no `--model` to `extra_args`. Cycle 4's claim "full suite green on opus-4-7" was materially false for this test. Cycle 6 will fix the fixture plumbing.
- **`test_standing_teammate_spawn` predicate matched `shutdown_request` prose.** The unanchored `entry_contains_text(e, r"ECHO:\s*ping")` regex fires on the FO's own SendMessage narration to echo-agent, not the ensign's ECHO reply. #185 audit miss; #188 landed narrower `entry_contains_text` arm scoped to subagent tool_result events.
- **FO-built checklists drifted 8-14 items per dispatch**, enumerating mechanical steps and diluting load-bearing signals. Fixed via #192 (≤3 linchpin items; boilerplate excluded).

## Issues — Spacedock

Not filed — captain deferred to next-session local investigation. Both captured in `_debriefs/_pending-notes.md`.

- **Context-exhausted subagent resisted shutdown.** #188 implementation ensign hit 101.8% of 200k context limit; first `shutdown_request` did not terminate promptly (continued idle notifications ~30+ min). Only terminated when paired with the fresh-dispatch replacement. Distinct from earlier-in-session shutdowns that worked within seconds.
- **`opus[1m]` model config silently falls back to `claude-opus-4-7`.** `claude-team context-budget` reported `config_declared_model: "opus[1m]"` with runtime `claude-opus-4-7` and a `config_drift_warning`. If the 1M-context variant silently degrades to 200k, we never get the budget the config requested. Source of the fallback is the `claude-team` helper itself.

## Observations

_(none recorded)_

## What's Next

**In flight at debrief time (not shipped):**
- **#186** (`green-opus-4-7-full-suite`) — cycle 6 queued: fix `test_gate_guardrail` model fixture + full-suite verify on post-#188 main.
- **#188** (`streaming-watcher-over-filesystem-polling`) — PR #127 CI approved and running; direct-merge path when green.

**Awaiting captain gate:**
- **#193** (`separate-ac-from-checklist-entity-vs-stage`) — ideation complete; staff reviewer pending before captain gate.
- **#187** (`refine-entity-as-folder-pr118`) — ideation at gate.

**Backlog (recommended next session):**
- **#194** — investigate opus-4-7 standing-teammate ECHO roundtrip flakiness; adjacent to #186 completion.
- **#190** — rewrite `test_feedback_keepalive` core assertion to cover both dispatch and inline-process paths (removes #185 cycle-3 xfail).
- **#191** — revisit commission skill behavior for first-time vs experienced users (product/UX).

**Local investigation (pre-captured in `_debriefs/_pending-notes.md`):**
- Context-exhausted subagent shutdown resistance.
- `opus[1m]` model config drift.

**Long-standing:**
- **#088** — PR #36 (restore initialPrompt) still open; untouched this session.

---
id: 118
title: Update pr-merge mod with rich PR body template
status: backlog
source: CL brainstorm during 2026-04-10 session — PR body thinness observed on #62
score: 0.80
---

PRs generated by the pr-merge mod now lead with motivation and end-user value, so reviewers and future debuggers see the "why" first and the audit metadata second. Previously, workflow-generated PR bodies were a single `Workflow entity: {title}` line that failed to surface any of the evidence already captured in stage reports.

## Problem

The pr-merge mod (`docs/plans/_mods/pr-merge.md`, currently v0.9.3) prescribes a minimal PR body: literally `Workflow entity: {entity title}` plus an optional `Closes {issue}`. This has been the rule since at least 0.9.0 (2026-04-02). Historical PRs from earlier FO sessions — #57, #56, #55, #53, #52, #51 — had richer bodies (Summary, Changes, Test plan sections) because those FOs went beyond the mod's minimum. PR #62 (task 115, this session) strictly followed the mod and produced a one-liner body, which prompted this task.

The entity file already contains everything a good PR body needs:
- **Problem statement** (entity body head) → motivation
- **Ideation stage report** → ACs and test plan
- **Implementation stage report** → concrete changes + self-test evidence
- **Validation stage report** → independent rerun confirmation

The mod just needs to tell the FO how to extract and arrange this material.

## Design (approved by captain during 2026-04-10 brainstorming)

### Template structure (top to bottom)

| Section | Required | Content |
|---|---|---|
| Motivation lead | **yes** | 1-2 sentences blending motivation (problem) + end-user value (what the reader gets) |
| `## What changed` | **yes** | Action-verb bullets, ≤ 6 |
| `## Evidence` | **yes when validation ran** | Test suites with `N/N passed` format; include quantitative results if stage reports called them out |
| `## Review guidance` | optional | 1 line pointing reviewer at the critical file or risky change — include only when a stage report explicitly flagged it |
| `---` separator + `Workflow entity: {title}` | **yes** | Audit metadata, at the bottom |
| `Closes {issue}` | **yes when issue set** | Under the Workflow entity line |
| `Related: {siblings}` | optional | Under Closes, only when stage reports flagged follow-ups |

### Extraction rules (the mod must specify these so the FO can apply deterministically)

| PR body section | Source in entity file | Transformation |
|---|---|---|
| Motivation lead | Entity body paragraph(s) between closing `---` and the first `##` heading | Condense first paragraph to 1-2 sentences. Lead with impact or action verb — not "This PR" or "This task". Blend motivation + value. |
| What changed | Implementation stage report's `[x]` DONE items | One action-verb bullet per meaningful unit. Collapse sibling bullets that describe the same thing. Drop `[x]` markers. |
| Evidence | Validation stage report items that assert AC verification (typically rerun-test items) | One bullet per suite with `N/N passed` format. Include any quantitative result the stage report explicitly called out (wallclock delta, size %, perf). Fallback to implementation report's self-test items (items 5-6 by convention) if no validation stage exists. |
| Review guidance | Explicit "focus on X" / "risk here" notes in either stage report | 1 line. **Omit if no such note exists.** |
| Workflow entity line | Entity title verbatim | Prefix `Workflow entity: ` |
| Closes | Entity frontmatter `issue` field (exactly as written) | Prefix `Closes ` |
| Related | Explicit "related task" / "follow-up" mentions in stage reports | 1 line. **Omit if none.** |

Target total length: **100-200 words**, matching PR #57's shape.

### Key design decisions

1. **Lead with motivation + end-user value.** First content is a 1-2 sentence user-facing impact statement. The `Workflow entity:` line moves to the bottom as audit metadata.
2. **Prescribed sections + extraction rules** — not a strict verbatim template, not free-form. The mod specifies headings and source subsections; the FO paraphrases rather than pasting.
3. **Evidence section is conditional on validation stage.** Non-validated workflows fall back to implementation self-test evidence.
4. **Review guidance and Related are opt-in.** They appear only when stage reports explicitly flagged them, to prevent bloat.

## Acceptance Criteria

1. The pr-merge mod's merge-hook section (`docs/plans/_mods/pr-merge.md`) is updated to specify the new PR body template structure and extraction rules verbatim as in this design. The `version` field is bumped.
   - Test: `git diff HEAD~1 -- docs/plans/_mods/pr-merge.md` shows the new template text; version bumped from `0.9.3`.
2. The mod text is internally consistent: the template structure section references the same section headings used in the extraction rules, and length guidance is present.
   - Test: manual inspection; optional static assertion in `tests/test_agent_content.py` that the mod file contains both a "template structure" and "extraction rules" block and that their section names match.
3. All other pr-merge mod behavior is preserved exactly — PR approval guardrail, push-main ordering, rebase step, gh pr create call, issue linking, pr field setting, archive-on-merge flow, startup/idle hook PR state checks.
   - Test: `unset CLAUDECODE && uv run tests/test_merge_hook_guardrail.py` stays green.
4. The canonical copy in the plugin source (if it exists separately from `docs/plans/_mods/pr-merge.md`) is updated in lockstep. No drift between canonical and installed copies.
   - Test: `diff` or `grep -l` between copies; identical content.
5. A worked example applied to PR #62 is included in the implementation Stage Report as an inline appendix: show the full PR body that the new template would produce for task 115's entity file. This is the spec's own self-test.

## Test Plan

- **Static inspection** of the mod file diff (manual).
- **E2E regression:** `tests/test_merge_hook_guardrail.py` stays green — low cost, required.
- **Worked-example dry run:** apply the new template to PR #62's entity file on paper (not actually editing the PR — that's the follow-up). Produce the resulting body string and include it in the Stage Report as proof.
- **Scope discipline check:** `git diff --stat` on the implementation commit must show only `docs/plans/_mods/pr-merge.md` (plus refit/installed copy if separate).

No new E2E tests. The mod change is static content verifiable by reading.

## Out of Scope

- **Updating PR #62 itself.** Captain's explicit ordering: "update the mod, before we update #62". PR #62 update is a follow-up after this lands.
- **Adding a GitHub PR template file** (`.github/PULL_REQUEST_TEMPLATE.md`). The mod is the Spacedock-workflow PR body source; the mod is the right surface.
- **Changing PR titles.** Current "title = entity title" rule is fine.
- **Changing anything else in the merge flow** — push order, rebase, gh pr create args, pr field, archive flow, hook wiring.

## Related

- Task 115 `fo-dispatch-template-completion-signal` — PR #62 is currently pending merge with a thin body; updating it is the direct follow-up once this mod change lands.
- Task 108 / PR #57 — the quality benchmark this design targets.

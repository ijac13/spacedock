---
id: "001"
title: Create a greeting file
status: backlog
score: 0.90
source: test
started:
completed:
verdict:
worktree:
---

Create a file `greeting.txt` in the repo root containing exactly `Hello, World!`.

## Acceptance Criteria

1. File `greeting.txt` exists at the repo root
2. File contains exactly `Hello, World!`
3. All tests in `tests/test_add.py` pass (run with `python3 tests/test_add.py`)
